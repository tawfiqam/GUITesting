<Page
    x:Class="CommandBar.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CommandBar"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:sys='clr-namespace:System;assembly=mscorlib'
    mc:Ignorable="d">
    <Grid x:Name="grid"  Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
       
        <StackPanel Grid.RowSpan="2" VerticalAlignment="Stretch" Height="Auto">
            <MediaElement x:Name="media"/>
        </StackPanel>
        <StackPanel Grid.Row="1" VerticalAlignment="Bottom">
            <StackPanel.Background>
                <LinearGradientBrush Opacity="0.4">
                    <GradientStop Color="Black"/>
                </LinearGradientBrush>
            </StackPanel.Background>
            <Border Background="Transparent" Width="200" Height="100" BorderBrush="LightBlue">
                <Canvas>
                    <Canvas.Resources>
                        <Storyboard x:Name="myStoryboard">
                            <DoubleAnimation
                                x:Name="myPointAnimation"
                                Storyboard.TargetProperty="Opacity"
                                Storyboard.TargetName="myRectangle"
                                From="1.0" To="0.0" Duration="0:0:5"/>
                        </Storyboard>
                    </Canvas.Resources>
                    <Rectangle x:Name="myRectangle" Width="100" Height="100" Fill="Blue" Stroke="LightBlue" 
                     Canvas.Top="20" Canvas.Left="20" StrokeThickness="3"/>
                </Canvas>
            </Border>
            <Slider Name="sliderSeek" ValueChanged="media_MediaOpened" KeyDown = "sliderSeek_Pointer_Change_Position" DragStarting="sliderSeek_Dragging_Start" DropCompleted="sliderSeek_Dragging_End"/>    
        </StackPanel>
    </Grid>
    <Page.BottomAppBar>
        <CommandBar x:Name="BottomCommand" Height="auto" RenderTransformOrigin="0.5,0.5" IsSticky="False">
            <AppBarToggleButton Label="Play" x:Name="PlayPause" Checked="Pause_Click" Unchecked="Play_Click" Icon="Pause"/>
            <AppBarToggleButton Label="ScreenSize" x:Name="LargeSmall" Checked="Full_Click" Unchecked="Small_Click" Icon="FullScreen"/>
            <AppBarSeparator/>
            <AppBarButton Icon="Back" Label="Back" Click="Back_Click"/>
            <AppBarButton Icon="Stop" Label="Stop" Click="Stop_Click"/>
        </CommandBar>
    </Page.BottomAppBar>
</Page>
